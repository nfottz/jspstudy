<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace는 mybatis.mapper.bbs.xml 파일을 의미한다.(확장자없이 작성) -->
<mapper namespace="mybatis.mapper.bbs">

	<!-- 목록보기 -->
	<select id="selectAllBbsList">
		SELECT BBS_NO, TITLE, CONTENT, MODIFIED_DATE, CREATED_DATE
		  FROM BBS
		 ORDER BY BBS_NO DESC
	</select>
	
	<!-- 상세보기 -->
	<select id="selectBbsByNo">
		SELECT BBS_NO, TITLE, CONTENT, MODIFIED_DATE, CREATED_DATE
		  FROM BBS
		 WHERE BBS_NO = #{bbsNo}
	</select>

	<!-- 삽입 -->
	<insert id="insertBbs">
		INSERT INTO BBS
			(BBS_NO, TITLE, CONTENT, CREATED_DATE)
		VALUES
			(BBS_SEQ.NEXTVAL, #{title}, #{content}, TO_CHAR(SYSDATE, 'yy.MM.dd HH:mm:ss'))
	</insert>

	<!-- 수정 -->
	<update id="updateBbs">
		UPDATE BBS
		   SET TITLE = #{title}
		   	 , CONTENT = #{content}
		   	 , MODIFIED_DATE = TO_CHAR(SYSDATE, 'yy.MM.dd HH:mm:ss')
  		 WHERE BBS_NO = #{bbsNo}
	</update>

	<!-- 삭제 -->
	<delete id="deleteBbs">
		DELETE
		  FROM BBS
		 WHERE BBS_NO = #{bbsNo}
	</delete>
	
</mapper>